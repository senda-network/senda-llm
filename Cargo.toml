[workspace]
members = [
    "closedmesh",
    "closedmesh/plugin",
    "closedmesh-client",
    "mesh-api",
    "mesh-host-core",
    "mesh-api-ffi",
    "closedmesh-test-harness",
    "tools/xtask",
]
default-members = [
    "closedmesh",
    "closedmesh/plugin",
]
resolver = "2"
